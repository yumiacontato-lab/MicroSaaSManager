---
status: unfilled
generated: 2026-01-15
---

# Glossary & Domain Concepts

List project-specific terminology, acronyms, domain entities, and user personas.

## Type Definitions
- **BadgeProps** (interface) — [`BadgeProps`](client/src/components/ui/badge.tsx#L28)
- **ButtonProps** (interface) — [`ButtonProps`](client/src/components/ui/button.tsx#L42)
- **CalendarProps** (type) — [`CalendarProps`](client/src/components/ui/calendar.tsx#L8)
- **ChartConfig** (type) — [`ChartConfig`](client/src/components/ui/chart.tsx#L11)
- **InsertSubscription** (type) — [`InsertSubscription`](shared/schema.ts#L137)
- **InsertWhatsappAlert** (type) — [`InsertWhatsappAlert`](shared/schema.ts#L151)
- **IStorage** (interface) — [`IStorage`](server/storage.ts#L17)
- **Subscription** (type) — [`Subscription`](shared/schema.ts#L138)
- **UpsertUser** (type) — [`UpsertUser`](shared/schema.ts#L120)
- **User** (type) — [`User`](shared/schema.ts#L121)
- **WhatsappAlert** (type) — [`WhatsappAlert`](shared/schema.ts#L152)

## Enumerations
- *No enums detected.*

## Core Terms

Define key terms, their relevance, and where they surface in the codebase.

## Acronyms & Abbreviations

Expand abbreviations and note associated services or APIs.

## Personas / Actors

Describe user goals, key workflows, and pain points addressed by the system.

## Domain Rules & Invariants

Capture business rules, validation constraints, or compliance requirements. Note any region, localization, or regulatory nuances.
