import Dashboard from "./dashboard";

// For now, subscriptions page is the same as dashboard
// In the future, this could be a dedicated list view with more filtering options
export default function Subscriptions() {
  return <Dashboard />;
}
